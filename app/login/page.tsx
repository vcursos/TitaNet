import { LoginForm } from '@/components/auth/login-form'

export const dynamic = 'force-dynamic'
export const metadata = { title: 'Entrar | TitaNet' }

export default function LoginPage() {
  return <LoginForm />
}
